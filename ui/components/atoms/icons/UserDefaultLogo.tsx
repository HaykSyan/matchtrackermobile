import { SvgProps } from 'react-native-svg';
import UserAvatarSvg from '../../../../assets/avatar_global.svg';
const UserDefaultLogo: React.FC<SvgProps> = (props) => <UserAvatarSvg {...props} />;

export default UserDefaultLogo;
